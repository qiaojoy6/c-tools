---
description: 功能文档只记功能清单；修改前清理无用代码
alwaysApply: true
---

# 功能文档与代码清理

1. 在简单的方法添加简单的注释， 比较复杂的方法，需要在方法体中必须要逻辑添加注释
2. 改动代码，注释对应的方法正确的话就不要删除注释，只有不对的话将注释更新，不要删除

## 1. 功能文档（只记有什么）

- 写在 `docs/README.md`：按模块分节，功能条目 + 相关文件路径，不写实现细节
- **新增功能**：在对应模块节补一条；涉及新文件则补到该节「相关文件」；新模块则在 README 中新增一节

## 2. 修改前先清无用代码（强制）

改功能或修 bug 时：

1. **排查**：旧实现、废弃分支、临时 debug、无调用的 IPC/preload/导出
2. **删除**：确认无用则直接删，并同步去掉失效引用
3. **禁止**：新旧两套实现长期并存

### 新增模块

1. 每次新增内容需要按照模块进行划分，每个文件的尽量不要超过300行代码（不是必须）

临时 debug（`reportDebug`、`.dbg` 等）验证通过后同一变更内删除。
